{
  "main": "bmi.js",
  "scripts": {
    "test": "jest --coverage",
    "coveralls": "jest --coverage && cat ./coverage/lcov.info | coveralls"
  },
  "devDependencies": {
    "coveralls": "^4.3.0",
    "jest": "^27.0.4"
  }
}

