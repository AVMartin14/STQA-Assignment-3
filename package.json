{
  "main": "bmi.js",
  "scripts": {
    "test": "jest --coverage",
    "coveralls": "jest --coverage && cat ./coverage/lcov.info | coveralls"
  },
  "dependencies": {
    "readline": "^1.3.0"
  },
  "devDependencies": {
    "coveralls": "^3.1.1",
    "jest": "^29.5.0"
  }
}

