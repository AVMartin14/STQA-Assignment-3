{
  "main": "bmi.js",
  "scripts": {
    "test": "jest --coverage",
    "coveralls": "jest --coverage && cat ./coverage/lcov.info | coveralls"
  },
  "dependencies": {
    "readline": "^1.3.0"
  },
  "devDependencies": {
    "coveralls": "^4.3.0",
    "jest": "^27.0.4"
  }
}

